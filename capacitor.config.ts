import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.reactprototype',
  appName: 'React Prototype App',
  webDir: 'build'
};

export default config;
